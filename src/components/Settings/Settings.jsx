import React from 'react';
const ThemeContext = React.createContext('light');

const Settings = () => {
    return (
        <div>
            Settings
        </div>
    )
}

export default Settings;